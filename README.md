# Ultrasonic Distance Monitor

A Flutter app that displays real-time ultrasonic distance measurements using Firebase Realtime Database.  
Features include a responsive and modern UI, error handling, and support for light/dark themes.  
To get started, clone the repository, install dependencies with `flutter pub get`, configure Firebase in `main.dart`, and run the app using `flutter run`.  
Compatible with both mobile and web platforms.
